<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>puluwrite</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/v/bs5/jq-3.7.0/dt-2.3.4/datatables.min.css">
    <style>
        @font-face {
            font-family: 'Playpen Sans Variable';
            font-style: normal;
            font-display: auto;
            font-weight: 100 800;
            src: url(https://cdn.jsdelivr.net/fontsource/fonts/playpen-sans:vf@latest/latin-wght-normal.woff2) format('woff2-variations');
            unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
        }

        * {
            font-family: 'Playpen Sans Variable', sans-serif;
        }

        .ProseMirror {
            border: var(--bs-border-width) var(--bs-border-style) var(--bs-border-color);
            padding: 0.5rem;
        }

        .ProseMirror-focused {
            outline: none;
        }

        @media (min-width: 1280px) {
            .ProseMirror {
                padding-right: 20rem;
            }
        }
    </style>
</head>

<body>
<div class="bg-body-secondary py-2 mb-3">
    <div class="container-lg d-flex justify-content-between align-items-center px-4">
        <h1 class="fs-5 mb-0">puluwrite
            <span class="fs-6 small">v0.1</span>
        </h1>
        <div class="dropdown">
            <button type="button" class="btn" data-bs-toggle="dropdown">
                Hello, {{ session.user.name }}
            </button>
            <div class="dropdown-menu" style="z-index: 2000">
                <a href="/password" class="dropdown-item">
                    <i class="bi-lock"></i> Change password
                </a>
                <div class="dropdown-divider"></div>
                <a href="/logout" class="dropdown-item">
                    <i class="bi-box-arrow-right"></i> Logout
                </a>
            </div>
        </div>
    </div>
</div>
{% block main %}{% endblock %}

{% if hasFlash('success') %}
    <div class="toast-container position-fixed bottom-0 end-0 p-2">
        <div class="toast bg-success-subtle">
            <div class="toast-body">
                {{ getFlash('success') }}
            </div>
        </div>
    </div>
{% endif %}

{% if hasFlash('error') %}
    <div class="toast-container position-fixed bottom-0 end-0 p-2">
        <div class="toast bg-danger-subtle">
            <div class="toast-body">
                {{ getFlash('error') }}
            </div>
        </div>
    </div>
{% endif %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.datatables.net/v/bs5/jq-3.7.0/dt-2.3.4/datatables.min.js"></script>
<script>
    document.querySelectorAll('.toast').forEach(toast => {
        bootstrap.Toast.getOrCreateInstance(toast).show();
    });
</script>
{% block scripts %}{% endblock %}

</body>

</html>
